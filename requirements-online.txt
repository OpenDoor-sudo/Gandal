# Desktop online test (Gemini Flash Live + LiveKit).
# LIVEDOCS core: livekit-agents, livekit-plugins-google, livekit-plugins-openai,
# lancedb, python-dotenv.
# Do NOT install sentence-transformers, torch, or livekit-plugins-silero here.
# Gemini Live uses Google cloud VAD (LIVEDOCS Patch #1 mocks local_inference).
# Silero pulls onnxruntime (~24MB) and upgrades livekit-agents as a side effect.
websockets>=12.0
python-dotenv>=1.0.0
requests>=2.31.0
pypdf>=4.0.0
lancedb>=0.6.0
pyarrow>=15.0.0
livekit>=1.0.0,<2
livekit-agents>=1.8.0,<1.9
livekit-plugins-google>=1.8.0,<1.9
livekit-plugins-openai>=1.8.0,<1.9
